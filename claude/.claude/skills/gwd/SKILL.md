---
name: gwd
description: Slash-command shortcut for grill-with-docs. Triggers ONLY on explicit /gwd invocation. Auto-trigger phrasing ("grill me", "stress-test this design", etc.) goes to grill-with-docs directly, not here.
---

# /gwd — alias for grill-with-docs

The user typed `/gwd` as a shortcut. Immediately invoke the `grill-with-docs` skill via the Skill tool and hand off to it. Do not ask any clarifying questions before invoking — that's grill-with-docs's job.

After the Skill tool loads grill-with-docs, follow its instructions exactly. This file's only purpose is to forward.
