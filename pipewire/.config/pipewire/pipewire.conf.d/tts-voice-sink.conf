context.objects = [
    {   factory = adapter
        args = {
            factory.name     = support.null-audio-sink
            node.name        = "tts-voice-mustang"
            node.description = "TTS Voice — Mustang"
            media.class      = "Audio/Sink"
            audio.position   = [ FL FR ]
            adapter.auto-port-config = {
                mode = dsp
                monitor = true
            }
        }
    }
    {   factory = adapter
        args = {
            factory.name     = support.null-audio-sink
            node.name        = "tts-voice-velise"
            node.description = "TTS Voice — Velise"
            media.class      = "Audio/Sink"
            audio.position   = [ FL FR ]
            adapter.auto-port-config = {
                mode = dsp
                monitor = true
            }
        }
    }
]
